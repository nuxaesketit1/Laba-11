#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {

setlocale(0, "");

int a, b;

do{

cout << "Введите последовательно числа A и B так, что (A < B) : " << endl;
cin >> a >> b;

  if (a < b) {

    for (int i = a; i < (b + 1); i++) {
      for (int j = 1; j < (i + 1); j++){
        cout << setfill(' ') << setw(3) << i << ends;
      }
      cout << endl;
    }

    break;

  } else {
    cout << "Повторите ввод данных!" << endl;
  }
} while(true);

cout << endl;
system("pause");
return 0;

}
